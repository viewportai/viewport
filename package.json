{
  "name": "@viewportai/viewport-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "services/relay",
    "integration"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "check": "npm run check --workspaces --if-present",
    "format:check": "npm run format:check --workspaces --if-present",
    "daemon": "npm run dev -w @viewportai/daemon",
    "relay": "npm run dev -w @viewportai/relay",
    "daemon:test": "npm run test:unit -w @viewportai/daemon",
    "relay:test": "npm run test -w @viewportai/relay",
    "version:packages": "npx @changesets/cli version",
    "release:packages": "npx @changesets/cli publish",
    "daemon:check": "npm run check -w @viewportai/daemon",
    "relay:check": "npm run check -w @viewportai/relay",
    "ops:integration:operator": "npm run test:operator -w @viewportai/integration",
    "ops:integration:e2e": "npm run test:e2e -w @viewportai/integration",
    "ops:relay:container": "bash ./scripts/start-relay-container.sh"
  }
}
